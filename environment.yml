name: deepfake

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.11
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - pip

  - pip:
      - opencv-python==4.10.0.84
      - pillow
      - tqdm
      - torch==2.11.0
      - torchvision==0.26.0
      - kaggle
